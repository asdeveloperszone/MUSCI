<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="horizontal" android:gravity="center_vertical"
    android:background="@drawable/bg_widget" android:padding="10dp">

    <ImageView android:id="@+id/widgetArt"
        android:layout_width="50dp" android:layout_height="50dp"
        android:src="@drawable/ic_music_note" android:scaleType="centerCrop"/>

    <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:orientation="vertical"
        android:layout_marginHorizontal="10dp">
        <TextView android:id="@+id/widgetTitle"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="No song playing" android:textColor="#FFFFFF"
            android:textSize="13sp" android:textStyle="bold" android:singleLine="true"/>
        <TextView android:id="@+id/widgetArtist"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Tap to open" android:textColor="#AAFFFFFF"
            android:textSize="11sp" android:singleLine="true"/>
    </LinearLayout>

    <ImageButton android:id="@+id/widgetBtnPrev"
        android:layout_width="36dp" android:layout_height="36dp"
        android:src="@drawable/ic_skip_previous" android:tint="#FFFFFF"
        android:background="?attr/selectableItemBackgroundBorderless"/>

    <ImageButton android:id="@+id/widgetBtnPlay"
        android:layout_width="40dp" android:layout_height="40dp"
        android:src="@drawable/ic_play" android:tint="#FFFFFF"
        android:background="?attr/selectableItemBackgroundBorderless"/>

    <ImageButton android:id="@+id/widgetBtnNext"
        android:layout_width="36dp" android:layout_height="36dp"
        android:src="@drawable/ic_skip_next" android:tint="#FFFFFF"
        android:background="?attr/selectableItemBackgroundBorderless"/>

</LinearLayout>
