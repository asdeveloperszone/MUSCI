<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_widget"
    android:padding="10dp">

    <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:orientation="vertical"
        android:layout_marginEnd="8dp">
        <TextView android:id="@+id/widgetTitle"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="No song playing" android:textColor="#FFFFFF"
            android:textSize="13sp" android:textStyle="bold"
            android:singleLine="true" android:ellipsize="end"/>
        <TextView android:id="@+id/widgetArtist"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Open Music Player" android:textColor="#AAFFFFFF"
            android:textSize="11sp" android:singleLine="true" android:ellipsize="end"/>
    </LinearLayout>

    <ImageButton android:id="@+id/widgetBtnPrev"
        android:layout_width="38dp" android:layout_height="38dp"
        android:src="@drawable/ic_skip_previous"
        android:tint="#FFFFFF" android:background="#00000000"/>

    <ImageButton android:id="@+id/widgetBtnPlay"
        android:layout_width="42dp" android:layout_height="42dp"
        android:src="@drawable/ic_play"
        android:tint="#FFFFFF" android:background="#00000000"/>

    <ImageButton android:id="@+id/widgetBtnNext"
        android:layout_width="38dp" android:layout_height="38dp"
        android:src="@drawable/ic_skip_next"
        android:tint="#FFFFFF" android:background="#00000000"/>

</LinearLayout>
